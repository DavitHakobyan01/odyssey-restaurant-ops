/**
 * Stats HTTP routes.
 *
 * PLACEHOLDER — filled in immediately after the codegen loop is verified.
 * Follows the reference pattern in menu.routes.ts.
 */
import type { OpenAPIHono } from '@hono/zod-openapi'

import type { AppEnv } from '../env'

export function registerStatsRoutes(_app: OpenAPIHono<AppEnv>) {
  // Routes registered in the next step.
}
